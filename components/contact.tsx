import React from "react";

type Props = {};

const Contact = (props: Props) => {
    return (
        <section id="contact">
            <div></div>
        </section>
    );
};

export default Contact;
